# terraform.tfvars
table_bucket_name = "future-ob-table-bucket"
namespace         = "rawdata"
table_name        = "future-orderbook"
# region is already defaulted to ap-south-1 in variables.tf, override if needed:
# region = "ap-south-1"
